import Foundation

// MARK: - Axiom Framework

/// Architectural framework for iOS development with actor-based state management
public struct AxiomFramework {
    /// Framework version
    public static let version = "2.0.0"
    
    /// Framework description
    public static let description = """
        Actor-based state management framework for iOS with:
        • Type-safe architecture with compile-time validation
        • Thread-safe state management through actors
        • SwiftUI integration with reactive binding
        • Minimal boilerplate through code generation
        • One macro + one protocol per component design
        """
}